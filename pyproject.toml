[project]
name = "agents"
version = "0.1.0"
description = "User-level agent skills: /go orchestration, project-memory, and the campaign loop"
requires-python = ">=3.11"

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
