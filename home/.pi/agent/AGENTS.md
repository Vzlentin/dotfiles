# Global User Preferences

- When spawning subagents, use a Herdr side pane with a live transcript when `HERDR_ENV=1`.
- Preserve the user's focus while launching Herdr subagents unless the user explicitly asks to switch context.
- When `HERDR_ENV` is not set to `1`, use the normal subagent mechanism.
